# 📱 Каталог телефонов — Django проект

Веб-приложение на Django, позволяющее просматривать каталог телефонов, сортировать товары и открывать их детальные карточки.
Проект использует данные, импортируемые из файла CSV.

### 📦 Функциональность
### 🔍 Каталог телефонов

- Страница /catalog/ отображает список всех телефонов:

- фото

- название

- цена

Пример интерфейса каталога:
```
📱 Samsung Galaxy Edge 2 — 73 000 ₽  
📱 iPhone X — 80 000 ₽  
📱 Nokia 8 — 20 000 ₽

```

### 🔄 Сортировка каталога

Поддерживаются три вида сортировки:

| Ссылка            | Описание                       |
| ----------------- | ------------------------------ |
| `?sort=name`      | сортировка по названию (A → Z) |
| `?sort=min_price` | сначала дешёвые                |
| `?sort=max_price` | сначала дорогие                |


Пример запроса:
```bash
/catalog?sort=min_price
```
### 📄 Страница телефона (/catalog/<slug>/)

Каждая модель телефона открывается через автоматически сгенерированный slug.

На странице отображаются:


- название

- цена

- дата выхода

- изображение

- поддержка LTE

### 📥 Импорт данных из CSV

Проект содержит специальную команду для загрузки CSV-файла:
```
python manage.py import_phones
```
CSV должен содержать:

id;name;image;price;release_date;lte_exists

### 📂 Структура проекта
```
phone_store/
│
├── manage.py                     # Основной Django CLI
├── db.sqlite3                    # База данных SQLite
├── phone.csv                     # CSV с данными телефонов
│
├── phone_store/                  # Главный Django-проект
│   ├── __init__.py
│   ├── asgi.py
│   ├── settings.py               # Настройки проекта
│   ├── urls.py                   # Главные URL маршруты
│   └── wsgi.py
│
└── phones/                       # Приложение "phones"
    ├── __init__.py
    ├── admin.py                  # Регистрация моделей в админке
    ├── apps.py
    ├── models.py                 # Модель Phone
    ├── views.py                  # Логика отображения каталога и деталей
    ├── tests.py
    │
    ├── templates/
    │   ├── catalog.html          # Страница каталога с сортировкой
    │   └── product.html          # Страница конкретного телефона
    │
    ├── management/
    │   └── commands/
    │       └── import_phones.py  # Команда: python manage.py import_phones
    │
    └── migrations/
        ├── __init__.py
        └── 0001_initial.py       # Миграции модели Phone
│
├── requirements.txt              
├── README.md

```


## 🖼 Скриншоты
### 📌 Каталог телефонов
![Каталог телефонов](Снимок%20экрана%202025-11-30%20173444.png)
## 🚀 Запуск проекта
1. Установить зависимости:
```
pip install -r requirements.txt
```
2. Применить миграции:
```bash
python manage.py migrate
```
3. Импортировать данные:
```bash
python manage.py import_phones
```
4. Запустить сервер:
```bash
python manage.py runserver
```
Проект откроется по адресу:

```cpp
http://127.0.0.1:8000/
```
### 🛠 Работа с моделями
После изменения моделей необходимо выполнить:

```bash
python manage.py makemigrations
python manage.py migrate
```

